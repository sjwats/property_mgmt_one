<%= simple_form_for @building do |f| %>
  <%= f.input :street_address %>
  <%= f.input :city %>
  <%= f.input :state, collection: Building.usa_states, include_blank: false %>
  <%= f.input :postal_code %>
  <%= f.input :description, as: :text %>

  <%= f.submit 'Add Building' %>

<% end %>
